%h1 Tags
%div.list-group.mb-3
  - @tags.each do |tag|
    = link_to tag.name, posts_path(q: {title_or_tags_name_or_categories_name_cont: tag.name}), class: "list-group-item list-group-item-action" 